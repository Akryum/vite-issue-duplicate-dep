{
  "name": "vite-duplicated-dep",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "refresh": "rm -rf ./node_modules/ && rm pnpm-lock.yaml && pnpm i && nr dev"
  },
  "dependencies": {
    "vpkg": "file:./vpkg"
  },
  "devDependencies": {
    "vite": "^3.0.7"
  }
}